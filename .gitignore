# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Secrets / local config
.env
*.env
.claude/

# MuJoCo / sim runtime + generated artifacts
MUJOCO_LOG.TXT
assets/menagerie_go2/_scene_sim.xml
assets/menagerie_go2/_signtex_*.png

# OS / editor junk
.DS_Store
*.ses
:memory:*

# Large recordings (regeneratable)
*.gif
!assets/readme/*.gif

# Room GLBs — big binaries; only bigroom.glb (the one scene's room) is tracked.
assets/replica/*.glb
!assets/replica/bigroom.glb

# Private working material (local only)
/private/

# Outreach / strategy notes (local only)
/docs/
